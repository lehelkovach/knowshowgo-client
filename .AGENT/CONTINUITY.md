# Continuity digest — knowshowgo-client

**Updated:** 2026-09-03 · Agents: read this at session start; keep it short.

## Now

- Policy is root [`AGENTS.md`](../AGENTS.md). Pair with KSG **`main`** (`v0.2.10`).
- Unified board: KSG [`docs/DEVELOPMENT-PLAN.md`](https://github.com/lehelkovach/knowshowgo/blob/main/docs/DEVELOPMENT-PLAN.md) v6.9. This repo is **K0** on that board.
- `seed_procedure_run_primitives` (K5) ships in this tag — KSG `test:ladder` fails against an older client.
- Additive wrappers: `evaluatePrototypeMatchList` / `cast_object`. One-pair
  `evaluatePrototypeMatch` stays. Not WTA. No `/api3`.
- Gitflow after this cut: branch from **`dev`**, PR into **`dev`**. Next `0.2.11-dev`.

## Holds

- This tag is `v0.2.10-client`. Next work lands on `dev` as `0.2.11-dev`.
- OSLO B1/C1 are not this repo.

## Anti-drift

Keep this file short. Do not keep a second ladder here.
