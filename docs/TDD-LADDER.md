# TDD ladder — knowshowgo-client (`main` / release)

**You are on `main`.** Integration is **`dev`**. `dev` is not `master`.

Canonical process:
[`knowshowgo/docs/TDD-LADDER.md` on `dev`](https://github.com/lehelkovach/knowshowgo/blob/dev/docs/TDD-LADDER.md).

This repo’s slices: [`docs/TDD-LADDER.md` on `dev`](https://github.com/lehelkovach/knowshowgo-client/blob/dev/docs/TDD-LADDER.md).

Do **not** start Influence rungs or rebase #44 from this tip. Branch from `dev`.
Object SDK (S3) is already shipped here. Prod = paired tag with KSG, not “merged to main.”
